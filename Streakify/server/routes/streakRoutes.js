// const express = require('express');
// const router = express.Router();
// const streakController = require('../controllers/streakController'); // Import streakController

// // Placeholder: Get streak data for a specific habit in a given month and year
// router.get('/streak-data', streakController.getStreakDataForMonth); 

// // Placeholder: Create a new streak (usually done when a new habit is created for a user)
// router.post('/', streakController.createStreak);

// // Placeholder: Get streak information for a specific user and habit
// router.get('/:streakId', streakController.getStreak);

// // Placeholder: Update streak data (e.g., increment current streak, update longest streak)
// router.put('/:streakId', streakController.updateStreak);

// // Placeholder: Delete a streak (this can happen when a habit is deleted, or the streak is reset)
// router.delete('/:streakId', streakController.deleteStreak);

// module.exports = router;
